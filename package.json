{
  "name": "delta-force-s10-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "update:data": "node scripts/collect-latest.mjs",
    "check": "node --check scripts/collect-latest.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
